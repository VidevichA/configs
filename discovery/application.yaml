eureka:
  instance:
    hostname: localhost
    metadata-map:
      zone: zone1
  client:
    fetch-registry: false
    register-with-eureka: false
    region: region1
    availability-zones:
      region1: zone1
  server:
    renewal-percent-threshold: 0.85
